# AI roles 最终审查修复报告

日期：2026-08-18

## 已完成

- `claude-bridge` 移除 `sh -c`，使用 `spawn(executable, argv)` 和真实 Linux FIFO fd；测试支持 `commandPath/commandArgs`，覆盖 shell 元字符 marker 不执行。
- `RoleStore.list()` 校验安全目录名及 `role.json.name` 与目录名一致性，不合规目录告警并跳过。
- `AiRolesService` 增加角色存在性校验、同角色 Promise 串行队列、失败后继续、删除状态保护，避免不存在角色建孤儿目录和迟到完成写历史。
- `ClaudeBridge.stop()` 捕获当次 PID/generation、结束 active turn，延迟 SIGKILL 不重新读取 PID 文件。
- 服务端 role list/delete/history/chat 增加错误处理；角色 chat 错误返回 `chatResponse`，`requestId` 在 chunk/response 透传。
- 前端角色 tab 改为 DOM/textContent/dataset/addEventListener，避免角色名进入 HTML/inline onclick；发送请求生成并校验 requestId，切 tab 后迟到消息不会污染当前上下文。
- 更新 `/mnt/AASC/docs/design/ai-roles.md`、`/mnt/AASC/docs/spec/ai-roles.md`、`/mnt/AASC/docs/todo.md`、`/mnt/AASC/changelog.md`。

## 测试结果

- `node --test src/apps/server/modules/ai-roles/*.test.js`：26 项通过。
- `node --check src/apps/server/boot/server-app.js`：通过。
- `node --check src/apps/web-mediacenter/ui/public/js/chat.js`：通过。
- `node --check src/apps/web-mediacenter/ui/public/js/websocket.js`：通过。
- 未启动真实 server 或 claude；FIFO 测试使用临时目录和测试 fake executable。

## 未完成项

- 无已知未完成的本次审查项。
- 当前工作区仍存在与本任务无关的 task-engine/workgroup/logs 改动，未修改、未暂存、未提交。
