# MMD AR 独立测试 APK / HTTPS 网页设计

## 状态

- 设计日期：2026-09-23
- 2026-09-24 增加同一测试页的 HTTPS 静态网页形态，发布到 `https://c.aasc.us/mnt/mmd-ar/`；原 APK 构建保留作回归基线。
- 2026-09-25 HTTPS 网页曾短暂切换为纯定位页；用户澄清后恢复完整 MMD 测试页，并保留网页专用的“定位标记后提前返回”测试分支。纯定位页是已被取代的中间版本。
- 2026-09-25 HTTPS 网页定位改用 MindAR Basic 的 A-Frame 目标锚点；原绿色中心点改为贴在目标平面上的半透明蓝色矩形与中心十字。原校准、IndexedDB 和 MMD 场景保留，独立 APK 与正式显示端不切换定位引擎。
- 2026-09-26 HTTPS 网页已发布本地图片模拟摄像头输入及四角透视控制；选图是被拍画面，不替代已保存的定位图。控制点可独立显示/隐藏，隐藏后仍使用当前透视。
- 2026-09-26 同一 `web-dist` 支持挂载在深层内网目录或外网 `/mnt/mmd-ar/`：页面、脚本、MMD 清单与模型资源都相对当前页面目录解析。模拟模式的“拍照校准”使用当前透视画布流，在拍照时冻结 960×540 一帧；真实模式仍请求实体相机。仅测试网页的拍后选区改为可整体拖动、沿边角缩放的轴对齐矩形，保存仍使用原 `selectedQuad` 与 IndexedDB 格式。
- 2026-09-26 电脑用模拟摄像头改为三个视角滑条：预览上方缩放、右侧竖向纬度、下方经度，拖动预览图片仅平移；重置恢复正面居中。四角由固定焦距下同一平面的姿态投影得到，取代任意四角拖动；拍后矩形选区是另一套独立交互。本次先更新本地测试网页，未发布外网。
- 2026-09-26 纬度滑条长度改以横向经度滑条的实际宽度和 960×540 模拟取景画面比例计算，并受预览区高度约束。网页定位图按水平地面处理：角色脚底固定在目标中心，MindAR 锚点相对相机的矩阵求逆后驱动独立 PMX 相机，投影矩阵同步；失锁时暂藏角色，停止恢复原视角。仅限测试网页，本轮未发布外网。
- 2026-09-26 随后修正模拟取景尺寸：选图后以横向 960 像素为基准，按原图宽高比计算画布和预览高度；纬度滑条也按原图比例计算。视频流、拍照源图和 MindAR 输入随画布变化。未选图时 960×540 仅为占位，比例超出 GPU 视口能力时明确报错。仅测试网页，未发布外网。
- 2026-09-26 模拟定位在浏览器切后台时仍释放旧视频，回前台自动重建流并恢复原目标；真实摄像头不自动重启。AR 视觉比例改在相机坐标中换算，模型根节点位置/缩放及物理世界保持原样，只在失锁时暂改可见性。仅测试网页，本轮未发布外网。
- 2026-09-26 模拟图控件扩为四个方向：上方缩放、左侧经度透视、右侧纬度透视、下方 0–360° 画面内水平旋转。经纬度先决定三维平面投影，再绕取景中心旋转已投影图形；图片拖动仍只平移。竖向两个滑条均按原图比例适配预览高度，仅测试网页。
- 2026-09-26 失锁显示改为保留当前模型和最后相机视角，直到重新找到目标；蓝色锚点标识仍随 MindAR 失锁而隐藏。停止定位时恢复普通展示相机，模型根节点与物理仍不改写。仅测试网页。
- 2026-09-26 水平旋转进一步修正为在原图三维平面内先转动，再施加经纬度透视；X/Y 先按取景宽高比换算到相同长度单位，透视后再换回画面坐标。此前“投影后二维旋转”的试验实现已被替换。
- 2026-09-26 上述本地测试页改动已发布到外网 `/mnt/mmd-ar/`；仅替换与本地构建不一致的九个静态文件，脚本先于首页切换，PMX/纹理/VMD 等无差异模型资源保持原样。真机失锁重锁与透视观感仍待现场验收。
- 2026-09-26 测试页相机跟随加入可调平移/旋转死区与按帧时间缓动，定位面板“相机跟随”分类提供四个本地保存的滑条。相机距离 100% 沿用原始跟踪，50%–100% 只按相机到定位图中心的连线缩短相机位置向量，朝向、投影矩阵与模型根节点不改。首次锁定立即对齐，失锁冻结已显示的视角，重锁平滑继续；只在测试网页使用，未自动发布外网。
- 2026-09-26 上述相机跟随版测试页已发布外网：仅替换 `index.html`、`display-mmd.js` 和 `display-pmx-runtime.js`，其余本地构建资源与线上一致；真机相机观感仍待现场验收。
- 2026-09-26 相机跟随死区改为直接比较 MindAR 检测到的定位图位姿：平移按定位图归一化宽度、旋转按锚点角度分别过滤，之后才计算相机变换。相机距离仅作用于换算后的相机位置，不参与抖动判定；保留原有缓动、失锁冻结及测试网页限定范围。该修正已发布外网。
- 2026-09-26 针对蓝色定位框已贴图但角色相机仍停留旧位置的现象，测试网页在 MindAR `targetUpdate` 后继续即时同步 PMX；锚点可见期间逐帧核对最后成功采纳的锚点/投影矩阵和 PMX 活跃状态，漏帧或暂时未就绪时补偿提交。重锁必须等 PMX 成功接收位姿才提示已同步，停止时取消补偿循环。仅测试网页，本地修复待现场验收，未自动发布。
- 2026-09-26 HTTPS 测试网页的“定位图与校准”新增底面/立面切换，默认底面并在浏览器本地保存。底面沿用定位图中心对齐角色脚底；立面让定位图下边缘中点对齐角色脚底，并用已编译定位图高宽比换算图中心。切换时复用最后锚点姿态立即重算相机，模型根节点、布料物理和原 APK/正式显示端均不变。本次只本地构建，未发布。
- A-Frame 改造已发布到外网 `/mnt/mmd-ar/`；本地与线上 Chromium 模拟相机均完成首锁、丢失和自定义图编译重启。真实手机首锁和视觉对齐待现场验收。
- 状态：独立 APK 已加入当前 JS 跟踪器与 MindAR 1.2.5 的单摄像头 A/B 对比，并为 Android 启动阶段加入诊断和 Insets 空值降级；MindAR 编译回调缺失已修复并显示编译进度/启动错误，当前修正版已上传外网，实际相机图片跟踪数据和布料物理仍待现场验收
- 工程位于 `3rd/mmd-ar-test/`；原测试 APK 不依赖 AASC 服务、不替换正式 APK，但后续停止维护，继续维护 HTTPS 测试页。
- 2026-09-24 应现场 AO 对照需要，例外重建一次独立测试 APK：复用当前显示端网页源文件和本地模型资源，不恢复 APK 的长期维护或发布流程。
- 该例外 APK 已在 SM-N9500 内屏覆盖安装并启动，供当前 AO 差异现场复测；不代表 Android 13 或真实相机识别已验收。

## 1. 目标

### HTTPS 网页形态

当前模拟画面由所选原图比例决定，而不是固定 16:9：画布宽 960 像素，高度按解码原图比例取整；预览宽高比和左右竖向滑条长度使用同一原图比例，画布流直接作为校准与 MindAR 输入。下方水平旋转在原图三维平面内先发生，再计算经纬度透视；此前投影后直接旋转归一化画面的行为已废弃。下文关于 960×540 的描述属于此前版本或未选图占位状态。

HTTPS 网页继续保留完整 MMD 测试 harness：模型、Three.js/Ammo runtime、灯光/AO、动作物理、旋转交互和定位校准均正常初始化。网页专用定位流程使用 MindAR Basic 的 A-Frame `mindar-image-target` 锚点；找到目标时，半透明蓝色矩形及中心十字直接随目标平面移动、旋转和缩放；目标丢失时锚点自动隐藏。状态从锚点事件同步到原定位面板，不向 MMD 模型写入跟踪姿态。校准沿用独立视频流：真实模式用实体相机，模拟模式用当前透视画布；定位启动前释放校准流，再由 A-Frame 独占相应输入。停止、切换和页面离开均清理资源。APK 和正式显示端仍使用原流程。

当前网页在锚点标记之外接入独立 PMX 相机：目标图视为水平地面，其中心对应模型原有脚底中心；目标姿态经平面转正、模型高度换算与逆矩阵得到世界相机姿态，PMX 画布同步 A-Frame 的投影矩阵和视频裁切。定位不改变模型根节点的位置、缩放或旋转，已有动作与物理继续原样运行。目标丢失时保持最后视角和角色显示，重新找到后继续更新相机；停止时还原普通展示相机。模拟输入仅在切后台前正处于定位时回前台自动恢复；真实摄像头仍需手动启动。这个处理仅提供单图相对定位，不提供脱离目标图的世界级 SLAM。

2026-09-25 实拍回放中，MindAR 对 `testimg/target.jpg` 和三张手机照片均能锁定，自写 JS 三张均未得到稳定几何；HTTPS 测试网页仍选择 MindAR 单算法，不显示自写 JS 跟踪器选择。此前为排查定位而发布的纯定位网页不再作为当前页面；正式显示端和独立 APK 的生产/基线跟踪逻辑保持原样。静态回放只验证样本，不等同于真机连续视频的首锁和抖动结论。

没有实体摄像头的电脑可在 HTTPS 测试页选择“模拟摄像头”，从本地选一张作为相机画面的图片。图片仅在当前浏览器内解码，不上传或写入定位图存储。固定焦距的平面投影由经度、纬度、缩放和图片平移决定四角，独立 WebGL 画布再用逆单应映射生成 960×540 连续视频帧，交给现有 MindAR A-Frame 视频入口；灰色背景模拟视野外区域。上方缩放、右侧竖向纬度和下方经度滑条分别控制对应参数；鼠标拖动图片只平移，重置恢复正面、100% 缩放和居中。四角不能自由拖动，投影退化时保留上一次有效画面。切换真实/模拟输入先停止当前定位，停止和页面离开释放捕获轨道与绘制计时器。测试页的拍照校准在模拟模式下复用模拟视频帧，在真实模式下仍使用实体摄像头；拍照后默认四边各内缩 15% 的矩形，可拖动内部平移、拖动边或角改变尺寸，框选始终保持轴对齐且受照片边界限制。保存时换算为原有四点数据以兼容 IndexedDB 与 MindAR 编译；正式显示端及旧测试 APK 保留独立四角选区。已有内置官方定位图可直接用于无相机测试。模拟首锁用于开发回归，不等同真机识别验收；正式显示端与旧测试 APK 不加入模拟输入。

手机浏览器下，灯光/定位面板高度以动态可视高度和安全边为限，允许面板纵向触摸滚动；Canvas 仍独立禁止浏览器手势。下方定位操作可滚动到屏幕内。网页脚本和 PMX runtime 的 URL 带内容版本，避免页面更新后读取旧缓存。

2026-09-25 测试网页进一步将拍照校准弹窗限制在真实可视区和安全区内，预览等比例收缩，短屏时只滚动弹窗内容，关闭、拍照和保存操作保持可达；四角选区继续按画布矩形换算原图坐标。第一分类内新增唯一可见的“开始定位／结束定位”状态按钮，仍调用现有定位控制逻辑，原两个业务按钮隐藏但保留事件绑定。摄像头关闭、未选目标和启动失败必须显示正确状态；不改正式显示端或旧测试 APK 的界面。

测试网页的补光阴影是独立于主光的选择：主光默认保留自己的投影；补光可无阴影、沿用主光遮挡、或从自身方向生成第二张阴影贴图。沿用主光时补光不得填亮主光阴影；选择自身阴影时两盏灯可同时投影。补光关闭时不产生第二张阴影图。新语义只在 HTTPS 测试页显式启用，正式显示端继续按已有三选一来源运行；着色器补丁只附着到测试页 PMX 材质实例，不修改 Three.js 全局 shader。测试页 ESM 灯光模块使用内容指纹避免旧缓存混用。

2026-09-25 增加网页专用“主光阴影”开关，默认开启并使用既有本地灯光配置保存。关闭主光投影时，补光选择“沿用主光阴影”等同无阴影；选择“补光自己的阴影”仍独立生成补光阴影。两盏灯都不投影时停止阴影贴图计算。正式显示端和旧测试 APK 不显示该开关，也不改变原三选一行为。

MindAR 真机预览正常但不能定位的重点原因是取帧尺寸契约不一致：测试适配层曾用 `videoWidth/videoHeight` 建立 Controller，却没有同步 `<video>` 的 `width/height`，而 MindAR 1.2.5 的 InputLoader 用后者绘制输入图。2026-09-25 的后续修复不再把原始高分辨率直接送给 Controller，而是用等比例缩放的独立识别画布，尺寸限制在约 640×480 的像素预算内，并以不超过每 100 ms 一次的频率从原视频更新；原摄像头画面和 CSS 全屏 cover 预览保持不变。识别后的模型姿态写入同样按 100 ms 间隔限速，避免重复样本和高频包围盒计算占用点击事件。使用高分辨率同源官方目标的浏览器合成摄像头帧与真实手机摄像头分别验收，不把合成图通过误当作真机成功。

2026-09-25 曾开放 HTTPS 测试页专用识别分辨率档位 25%／35%／50%／75%／100%；A-Frame 锚点定位替换旧 Controller 后，网页不再显示该选项，识别使用相机原始帧。旧独立测试 APK 与正式显示端的输入策略不随网页变更。

当前测试网页已以平放定位图中心作为世界原点，用锚点逆矩阵和投影矩阵驱动相机，定位期间暂停手动环绕视角；失锁保留模型和最后相机视角，结束定位恢复手动视角。真机上仍需核对定位图比例、模型尺寸、画面裁切与连续跟踪的视觉对齐；这不是脱离定位图的空间 SLAM。

验证边界：整张 `testimg/mindar.JPG` 网页截图可作为模拟摄像头画面与内置官方目标匹配，但自动化直接轮询 MindAR 的首锁结果不能替代真实显示端的相机帧回调、模型跟随和按钮交互验收；静态画布流可能不产生视频帧回调。

测试网页随包提供 MindAR 官方文档的原始示例定位图，并在定位面板提供查看原图入口，供另一屏幕显示或打印。首次没有保存定位图时默认选中官方图；已有用户定位图和上次选择优先保留。官方图为只读内置项，不写入 IndexedDB、不能删除；两种图像跟踪器复用其原图和全图四角。资源来自本站同源静态文件，加载失败时不影响用户已有定位图；相机仍只在用户点击开始定位后申请。该入口已发布到外网 HTTPS 测试页，只出现在该网页，正式显示端及旧测试 APK 不带官方图。真机首锁效果待现场验收。

测试网页的灯光和定位面板各保留原入口，面板内使用独立可展开分类缩短手机长列表：灯光按基础光照、AO、主光、补光、边缘光 1、边缘光 2、物理分组；定位按定位图与校准、跟踪操作、体感环绕分组。每个面板默认只展开第一组，其余组可单独展开。AO、补光、两盏边缘光和体感环绕的原开关移到浅色分类标题行，放在展开按钮之前；开关的标签只包裹复选框，原描述文字位于占满剩余标题空间的展开按钮中，确保点文字展开/收起而不是切换开关。开合监听必须直接绑定分类按钮，不能依赖文档冒泡：原灯光和定位面板会截断内部点击事件。分类仅改变网页生成页的 DOM 与样式，不改变控件 ID、设置持久化、跟踪操作、正式显示端或测试 APK。

浏览器直接打开 `https://c.aasc.us/mnt/mmd-ar/` 测试默认米娅 PMX、图片定位、灯光、VMD/布料物理和拖动旋转，不安装 APK，也不运行 AASC 服务。网页现使用 MindAR Basic 的 A-Frame 1.5.0／MindAR 1.2.5 目标锚点；A-Frame 组件和官方 `.mind` 文件从版本固定的 HTTPS CDN 加载，MMD 模型与其运行时仍由构建资源提供。浏览器只在真实相机模式启动定位时请求摄像头许可，模拟模式不请求。

网页部署在 `/mnt/mmd-ar/` 子路径下，构建时只改生成副本的资源 URL；原显示端文件与 APK 本地路由不变。网页与 APK 的浏览器 origin 不同，原 APK 保存的定位图和灯光设置不会自动迁移，首次网页使用需要重新校准。实际识别成功率仍需手机现场验证。

HTTPS 测试页提供独立的模型加载进度条：PMX 主文件下载、纹理项完成、VMD 下载与模型/物理初始化分段展示，模型真正可显示才报 100%。百分比为分段权重进度，并非全部资源的精确字节占比；服务器没有提供长度时，保留阶段提示而不伪造下载字节数。进度回调为可选项，测试 APK 和正式显示端不传入，界面保持现状。

独立 HTTPS 页在视口或舞台尺寸变化时调用已有 `DisplayMmd.resize()`，WebGL 画布、相机宽高比和 AO 绘制缓冲随之调整。正式显示端的灯光面板标题后增加只读 Canvas 绘制缓冲宽×高像素，并由共用灯光脚本在画布变化时更新，HTTPS 页直接复用；这反映实际渲染分辨率，不是 CSS 视口大小或手机屏幕标称分辨率。沿用渲染器最多 2 倍像素比的性能上限。独立测试 APK 后续不再维护。

提供可独立安装启动的 Android 测试 APK，用于现场验证图片基准图 AR 与当前默认 PMX。页面、AR 跟踪代码、PMX 模型和所需配套资源全部随 APK 提供；运行时不访问 AASC 服务或公网。

测试对象为默认 `miya-default`：米娅 PMX、该模型声明的纹理资源和当前默认循环 VMD。只打包这一套模型资源，不包括其他 PMX/VRM、LLM、ASR、TTS 或 AASC Node 服务。

## 2. 保留的交互能力

- 图片拍摄、四角选区、定位图本地保存、摄像头跟踪和模型姿态叠加。
- 当前 PMX 灯光设置面板和本地持久化。
- PMX/VMD 运行动画与布料/刚体物理。
- 在模型上拖动旋转；点按与拖动的语义沿用现有显示端实现。
- 相机首次使用时由 Android 系统申请 CAMERA 权限；系统权限弹窗期间保留待授权请求，普通退出/切后台时停止跟踪并释放摄像头。

## 3. 架构

```text
3rd/mmd-ar-test/
├── Android 测试工程
│   ├── 仅绑定 127.0.0.1 的 APK 内置静态 HTTP 服务
│   ├── 单独测试 Activity 与相机授权
│   └── 本地静态 HTTP 服务只提供 MMD profile 和 APK assets
├── MMD AR 测试页面
│   ├── 复用 web-mediacenter 的 MMD、PMX、AR、跟踪及灯光模块
│   ├── 仅在测试 APK 内提供当前 JS / MindAR 跟踪器选择与指标面板
│   └── 仅提供模型舞台、定位面板和灯光面板
├── model-manifest.json
│   └── 当前默认 PMX 及配套资源的固定长度和 SHA-256
└── output/
    └── aasc-mmd-ar-test.apk
```

APK 启动时会在固定回环地址 `127.0.0.1:17836` 上启动静态 HTTP 服务，从 APK assets 提供页面、模型、纹理、动作和 WASM。固定 origin 确保跨启动保留 IndexedDB 定位图和本地灯光设置。WebView 只允许访问该回环 origin；服务不绑定 LAN 地址，也不启动 AASC/Node 服务。相机仅在网页请求视频捕获时申请 Android CAMERA 权限，WebChromeClient 只授权本地 origin 的视频捕获。

Android Activity 按窗口、本地资源服务、WebView 创建和页面加载分阶段启动。窗口内容挂载并获得焦点后才读取 `window.insetsController` 并请求沉浸式显示；若 controller 为空或厂商窗口 API 报错，恢复普通系统栏和默认内容布局后继续运行。同步启动异常会记录阶段与完整堆栈，并展示可截图、可选择文字的错误页；WebView 恢复失败或渲染进程退出时也显示诊断页。此诊断不改变相机权限时机或权限范围。

## 4. 资源边界

- 下载源使用已发布的 `miya-v1` 固定资源，构建前逐文件核对 manifest 声明的大小和 SHA-256。
- PMX、纹理与默认 VMD 存放在本地构建缓存并进入 APK assets；缓存和 APK 均不提交 Git。
- JS/CSS/Three.js/Ammo 仅从当前仓库现有显示端源码中按清单打包，避免复制维护另一份运行时实现。
- APK 内模型 URL 限于 `/api/mmd/static/` 固定前缀；HTTP handler 只接受 GET/HEAD 和静态 allowlist 路径，拒绝路径越界、非回环访问及外部资源回退。
- 测试 APK 固定内置 MindAR 1.2.5 的识别运行时代码和 Apache-2.0 许可证，构建下载缓存和 APK asset 均逐文件校验大小/SHA-256；运行时不访问 CDN。
- MindAR 运行时代码仅在用户选择 MindAR 时从 APK 本地资源动态加载；当前 JS 跟踪器仍为默认选项。两种算法不并行运行，不创建第二个摄像头流或 Three.js/MMD 场景渲染器，并复用当前 IndexedDB 中相同的参考照片和四角选区。MindAR 每轮将选区透视校正为独立目标图并重新编译；首锁时间包含首次本地 JS 模块加载，但不包含相机授权等待。
- 指标记录目标准备/编译耗时、识别首锁耗时、算法识别帧率、按时间加权的可见率、丢失次数和屏幕锚点 RMS 偏差。MindAR 不提供匹配置信度，测试页面明确不显示伪造的置信度值；锚点偏差需在目标静止时测量才可近似表示抖动。

### 4.1 跟踪器 A/B 对比范围

- 对比入口和 MindAR 适配器只注入到生成的 `3rd/mmd-ar-test/` 测试 APK 页面，不写入正式 `display.html` 或生产显示端 tracker。
- 一次只运行一个算法；每轮都使用同一保存目标、选区、视频层和现有 MMD 姿态映射。算法初始化计时从 tracker 启动开始，不含系统摄像头授权等待。
- 当前 JS 的“准备时间”统计参考图载入/特征提取；MindAR 统计选区矫正/目标编译。两者是各自引擎对应的目标预处理阶段，结果用于设备侧对比，不宣称算法内部操作完全相同。
- “识别帧率”按各 tracker 实际产出的新识别样本计数，MindAR 由其 `processDone` 回调计样本，避免把重复读取的旧姿态算成新帧；可见率按时间加权，减少两种引擎帧率不同造成的抽样偏差。
- MindAR 运行时状态不得在共享摄像头已就绪后继续显示“请求摄像头权限”：目标编译期间明确提示引擎准备阶段，启动后切换为寻找定位图，错误仍由共享 AR 状态机显示。
- MindAR `Compiler.compileImageTargets` 调用必须提供进度回调；进度同步到定位状态和 A/B 提示，编译/初始化失败时显示具体错误，禁止继续显示“正在寻找定位图”。
- 竖屏测试页左上提示需为右上灯光按钮及安全 Insets 留出完整宽度；长文案允许换行，横屏布局保持现状。该规则只作用于独立测试 APK 的生成页。

## 5. 明确不做

- 不包含 AASC server、Node Runtime、WebSocket、控制端、聊天、LLM、ASR、TTS、媒体上传或热更新。
- 不改正式显示端的默认启动流程和服务器 MMD 发布流程。
- 不包含除 `miya-default` 外的模型或模型选择功能。
- HTTPS 网页单独发布到 `/mnt/mmd-ar/`，不覆盖安装正式 APK，也不修改 Offline 服务更新清单。
- 不切换生产默认跟踪算法，也不在 Offline/正式 APK 引入 MindAR。

## 6. 验收标准

1. `npm run build:apk:mmd-ar-test` 可构建独立 APK，applicationId 为 `com.aasc.mmdartest`，与 AASC 正式/Offline APK 不同。
2. APK 离线启动后自动加载米娅 PMX 与默认 VMD；无服务器地址输入、连接提示或 AASC 服务进程。
3. 图片校准与本地跟踪可申请和使用摄像头；结束或切后台时释放轨道。
4. 灯光面板正常，PMX 布料/刚体物理继续运行，模型拖动旋转有效。
5. APK 只包含固定的默认模型资源；模型完整性检查通过，且 APK 中没有 Node 服务/额外模型。
6. 在真实 Android 设备上验证相机授权、WebGL、VMD/布料物理、灯光和旋转交互。
7. 同一设备分别运行两个 tracker，比较准备/编译、首锁、实际识别帧率、时间加权可见率、丢失次数和静止目标锚点偏差；确认 MindAR 资源全部来自 APK 本地。
8. MindAR 摄像头就绪后，编译期间提示“MindAR 准备中”而非“请求摄像头权限”；引擎启动后显示寻找定位图状态。
9. MindAR 目标编译持续回报进度；编译失败时显示错误并且不启动 Controller。
10. 竖屏窄视口下顶部说明可换行且不覆盖灯光按钮；横屏布局不发生回归。

## 7. 风险与处理

- WebView 本地页需被识别为安全来源：使用 `127.0.0.1` 回环来源，并由 WebChromeClient 只授予本地 origin 的摄像头视频捕获权限；静态服务仅绑定回环地址。
- 当前工作区不含 PMX 二进制：构建工具按固定资源清单下载，并在构建前校验长度/SHA-256；下载或校验失败即终止。
- Android 设备 WebView/图形驱动的 PMX 物理表现可能不同：测试 APK 保留明确状态提示，最终视觉与性能仍需真机验收。

## 8. 当前真机验证记录

- SM-N9500 / Android 9（API 28）通过 ADB 安装 `aasc-mmd-ar-test.apk`，包名为 `com.aasc.mmdartest`；停止旧实例后，单实例已切换到内屏 display 0 前台运行。
- APK 内回环 HTTP 服务可提供主页、`miya-default` profile、PMX 和默认 VMD，测试请求均返回 HTTP 200；访问经 ADB forward 验证，转发端口随后已移除。
- 内屏起初处于 OFF；发送唤醒键后 display 0 状态变为 ON。ADB 截图可见测试页面控件和米娅 PMX 模型，证明 WebView/WebGL 可见渲染通过。
- Insets 真机值为 top 24、right 48 CSS px（对应设备物理 Insets top 42、right 84 px）；灯光/定位控件按 Insets 移入安全区域。触摸日志确认两个按钮 DOM target 正确，灯光与定位面板均可打开/关闭，模型拖动旋转继续可用。
- 完整相机授权、图片校准/跟踪和布料物理验收仍待现场执行。
- 2026-09-24 状态/竖屏布局修正版 APK 大小 `13,620,354` bytes，SHA-256 `3409e3e6a182339118b5cc09a32ce3e5ca0aea46a6e9e5bbe45f980dc548174e`；已覆盖安装 SM-N9500 / Android 9/API 28 并在 display 0 前台运行。临时切换竖屏截图确认左上提示与右上灯光按钮无重叠；设备自动旋转设置已恢复。相机授权后的 MindAR 编译与首锁未实测。
- A/B 版内置 MindAR 1.2.5，编译缓存与许可证在 APK 中；现场对比结果尚未采集，当前生产显示端仍只使用原 JavaScript tracker。
- 若旧 Activity 仍存活时再以另一 display 启动一个 Activity 实例，固定端口会导致第二个实例绑定失败。测试时通过停止旧进程再单实例启动绕过；如何支持多实例/重复启动待确认后处理。

## 9. Android WebView 触摸安全区域

- Android 沉浸式窗口的绘制 frame 可能大于应用实际可用区域；控件布局不能只依赖 CSS `env(safe-area-inset-*)`，因为旧版 Android WebView 未必提供对应值。
- Activity 读取当前窗口 Insets，将实际安全距离按 WebView CSS 像素换算后写入页面 CSS 自定义属性；测试页的右上角灯光/定位控件据此向内偏移。
- Activity 记录右上区域原生触摸的 raw/local 坐标；测试页记录 DOM `pointerdown`/`click` 目标与面板状态，通过 WebChromeClient console 日志对照验证事件链；蓝色点击高亮不作为事件成功的唯一依据。
- 测试 harness 必须保留 `.display-interaction-layer` 的 `z-index: 50` 层叠上下文；只抽取内部按钮会使 `z-index: 10` 的交互 canvas 抢占 DOM hit-test。
- 修复仅作用于 `3rd/mmd-ar-test/` APK，不修改正式显示端共享 CSS/JS；验收需确认灯光面板和定位面板均可开合，模型拖动旋转仍有效。
- 根因：独立测试 harness 只抽取灯光/定位控件，没有保留正式页面 `.display-interaction-layer` 的 `z-index: 50` 层叠上下文，导致 `z-index: 10` 的 MMD canvas 抢占 DOM 命中。补回交互层包装后，pointerdown/click 均命中按钮；动态 Insets 同步同时解决右侧布局落入系统安全区的问题。

## 10. 跟踪器 A/B 版本设备冒烟

- 更新后的独立 APK 大小 `13,607,730` bytes，SHA-256 `7ece4967af75858667c1b300d8d9c5d2d893ee5cd08c4ed04094408cf508131c`；APK ZIP 完整性和 APK v2 签名验证通过。APK 内 14 个模型文件及 6 个 A/B 资源通过构建时内容验收。
- 已覆盖安装到 SM-N9500 / Android 9 / API 28 并在 display 0 启动；ADB 截图可见米娅 PMX、定位面板和算法选择器，能在“当前 JS”与“MindAR 1.2.5”之间切换。
- 经临时 ADB 回环转发读取 APK 内的 MindAR 入口脚本、Controller chunk 和 UI chunk，下载响应 SHA-256 与构建清单一致；临时转发已移除。LICENSE 已由 APK 内容验收校验，但本地静态服务不开放其 HTTP 路径。
- 本次未触发摄像头权限、未拍摄环境图，也未开始实际跟踪；因此没有首锁/识别帧率/可见率/丢失/锚点偏差的真机 A/B 数据，需使用可控纹理目标现场测试。
