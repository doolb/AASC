# 正式 APK 全模型服务器下载实施计划

## 目标

正式 `android-display` APK 不内置 ASR、TTS、声纹、RapidOCR、YOLO 和 GTCRN 模型；新增视觉/降噪模型清单与下载路由，APK 首次使用时从服务器校验下载，推理仍在显示端本机完成。

## 任务

1. [x] 盘点正式 APK 各模型现有来源、服务器路由和构建注入点。
2. [x] 更新 design/spec/task 文档，明确六类模型的统一分发边界。
3. [ ] 增加服务器模型清单、路径安全、APK 无内置模型的失败测试。
4. [ ] 实现服务器视觉和语音降噪模型清单/下载路由及 YOLO 准备命令。
5. [ ] 实现 APK 视觉和降噪按需下载、hash 校验、缓存和状态接入。
6. [ ] 移除 APK 视觉/降噪模型资产复制任务，支持可选 YOLO 模型 ID。
7. [ ] 构建、单测、HTTP 下载、真机截图链路验证，更新文档并提交 git。

## 约束

- 直接在 `/mnt/AASC` 的 master 工作区修改，保留无关用户改动。
- 服务端不执行视觉推理；服务器只做模型文件分发和已有视觉任务转发。
- 生成的 YOLO ONNX 不提交 git；通过 `/home/as/yolo11*.pt` 和准备命令生成服务器资源。
