# 任务记录：整理代码结构并清理历史目录

## 任务描述

- 需求来源：用户提出“整理 代码结构”。
- 目标：对齐既有分层架构，清理根目录历史残留目录，并把运行时资源统一归位到 `res/`。

## Design 需求

1. 根目录仅保留有效职责目录，移除历史迁移后遗留目录。
2. 资源文件统一遵循 `res/models`、`res/uploads`、`res/temp`、`res/certs` 规范。
3. 对目录治理文档追加“历史目录清理与资源归位”设计说明。

## Spec 设计

1. 在 `docs/spec/project-structure.md` 补充历史目录清理与资源归位伪代码。
2. 在实现文档中明确工具脚本默认资源路径应落在 `res/temp/asr/`。
3. 同步更新 changelog 与 todo 检查时间，保证文档闭环。

## 受影响的功能模块和代码

- 工具脚本：`src/scripts/asr-stress-test.js`
- 忽略规则：`.gitignore`
- 设计文档：`docs/design/project-structure.md`
- 实现文档：`docs/spec/project-structure.md`
- 任务状态：`docs/todo.md`
- 变更日志：`changelog.md`
- 目录结构：`res/uploads/`、`res/temp/asr/`、`res/temp/uploads/`

## 执行结果

1. 已将根目录 `uploads/*` 迁移到 `res/uploads/`。
2. 已将 `temp/asr-stress-sample.wav` 与 `temp/asr/*` 迁移到 `res/temp/asr/`。
3. 已删除根目录历史目录：`uploads/`、`temp/`、`models/`、`routes/`、`viewbind/`、`voice-display/`、`voice-display-node/`。
4. 已将 ASR 压测脚本默认样本路径调整为 `res/temp/asr/asr-stress-sample.wav`。

## 自测用例

1. 检查根目录不存在历史目录：`uploads/`、`temp/`、`models/`、`routes/`、`viewbind/`、`voice-display/`、`voice-display-node/`。
2. 检查 `res/uploads/`、`res/temp/asr/`、`res/temp/uploads/` 存在。
3. 检查 `src/scripts/asr-stress-test.js` 默认路径为 `res/temp/asr/asr-stress-sample.wav`。
4. 检查 `docs/design/project-structure.md`、`docs/spec/project-structure.md` 已追加本次策略。

## 兼容性测试

- 运行时 URL 路由仍保持 `/uploads/*`，服务端映射目录为 `res/uploads/`，对外接口保持兼容。

## 性能测试

- 本次以目录治理为主，无业务逻辑性能回归点。

## 风险评估

1. 风险：若外部脚本硬编码依赖根目录 `uploads/`、`temp/`、`models/`，可能在本地联调时报路径错误。
2. 缓解：保留统一入口文档并将默认脚本路径改为 `res/`，后续新增脚本按该规则实现。

## 预计工时

- 0.5 人时（目录归位 + 文档同步）。

## 不明确的需求记录

- 当前按“结构归位+文档闭环”执行；若后续需要进一步拆分 `res/uploads/`（按业务分桶），可在下一任务中补充。
