# MMD AR 定位图位姿死区

- 日期：2026-09-26
- 任务：把测试网页的相机防抖死区改为根据 MindAR 定位图自身的位姿变化判断。
- 设计：定位图平移以归一化图宽百分比计，旋转以角度计；先过滤锚点，再换算相机；距离调节及逐帧缓动保持原语义。
- 实现：更新 `docs/spec/mmd-ar-test-apk.md` 伪代码，修改 `display-pmx-runtime.js`，并扩充 `tests/mmd-ar-camera-runtime.test.js`。
- 影响范围：仅 MMD AR HTTPS 测试页的 PMX 相机模式；正式显示端、独立 APK 和模型根节点不变。
- 自测：网页构建、合成锚点位姿浏览器回归；真机弱光抖动与快速移动仍需现场确认。
- 兼容性：沿用已有四个设置及 localStorage 值，不迁移数据。
- 性能：每次锚点更新增加一次矩阵分解与合成；不增加逐帧图像处理。
- 风险：MindAR 位姿仍可能有大幅误检；本次只处理阈值内抖动，不做重定位异常值过滤。
- 预计工时：约 1 小时。
