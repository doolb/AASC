# 显示端播报层级与聊天语音自动发送

## 任务描述

将聊天 TTS 和天气详情放入 MMD 上方、聊天层下方的播报辅助层；聊天打开时隐藏仍在有效期内的天气详情和 TTS 文字，关闭后恢复未到期内容。同时修复聊天层语音识别只回显、不自动发送或不更新显示端聊天窗口的问题。

## 设计需求

- 层级：媒体 < MMD < 播报辅助层 < 聊天层。
- 聊天打开/关闭只影响播报文字和天气弹窗可见性，不停止音频。
- 聊天层打开期间语音识别直接发送到当前聊天对象/会话，绕过普通语音确认拦截。
- 语音聊天流式消息同时发送到控制端和发起语音的显示端。

## 影响模块

- `src/apps/web-mediacenter/ui/public/display.html`
- `src/apps/web-mediacenter/ui/public/js/display-stage.js`
- `src/apps/web-mediacenter/ui/public/css/display-mmd.css`
- `src/apps/web-mediacenter/ui/public/css/display.css`
- `src/apps/server/boot/server-app.js`
- `tests/display-chat-mmd.test.js`
- `tests/display-voice-ui.test.js`

## 自测用例

1. MMD、聊天 TTS、天气详情和聊天层按目标顺序叠放。
2. 天气详情显示期间打开聊天，弹窗隐藏；关闭聊天且未超时，弹窗恢复；超时后不恢复。
3. TTS 文字在聊天打开时隐藏，关闭后恢复未结束的文字；音频持续播放。
4. 聊天层打开后语音输入无需确认即可进入当前聊天，会话选择正确。
5. 显示端收到 `chatInput`、`chatChunk`、`chatResponse` 并更新聊天气泡，控制端同步收到消息。
6. 普通聊天关闭时保持原有唤醒词、确认和指令路由。

## 兼容性与风险

- 普通浏览器显示端和 Offline WebView 共用同一 DOM/WS 协议。
- 不改变天气/TTS 服务端生成和音频播放协议。
- 聊天层绕过确认仅限 `chatLayerVisible=true` 的显示端来源，控制端和普通语音不受影响。

## 预计工时

- 实现与测试：约 1 小时。

## 完成结果

- 已在 `display.html` 增加播报辅助层，将 TTS 文字和天气详情按“媒体 < MMD < 播报辅助层 < 聊天层”排列；天气弹窗使用 CSS 隐藏，保留原有超时，聊天关闭后未到期内容自动恢复。
- 已在服务端识别聊天层来源的显示端语音输入，跳过普通语音对话确认拦截，并通过 `sendVoiceChatUpdate` 同步控制端与来源显示端的聊天流。
- 已更新显示端层级、天气隐藏恢复和聊天语音自动发送契约测试。

## 后续缺陷修复

- 普通 `voiceCommand` 的 `response` 分支此前仍使用默认 `document.body` 宿主，导致普通 TTS 弹窗可能位于聊天层上方，且没有聊天打开抑制标记。
- 已将普通 response 弹窗改为追加到 `displayBroadcastLayer`，并设置 `suppressWhenChatVisible=true`，与天气详情弹窗统一处理。
- 已增加浏览器回归用例，实际验证普通 TTS 弹窗在聊天打开时隐藏、关闭后恢复，且生命周期计时不被重置。

### 普通聊天回复重复弹窗

- 根因：普通语音聊天完成时，无论来源端是否支持 `voicePlayback`，服务端都会发送 `voiceCommand/action=response`；有语音能力的显示端同时收到 `tts/playAudio`，于是出现 TTS 播报文字和 response 弹窗两份内容。
- 修复：复用服务端 `hasManualVoicePlayback(displayId)` 判断来源端能力；有语音能力时只保留 TTS 和聊天气泡，无语音能力时保留 response 文字兜底。
- 不改变实际 TTS 目标选择；来源端无能力但其他显示端有能力时，仍允许其他显示端播放，同时来源端显示文字兜底。

### 天气播报重复弹窗

- 根因：天气查询完成后，服务端既通过 `onTts` 发送 TTS，又无条件通过 `weatherResult` 发送天气详情弹窗；有语音能力的来源端因此同时显示播报文字和天气详情。
- 修复方案：复用来源显示端 `voicePlayback` 能力判断；有能力时只保留 TTS，来源端无能力时保留 `weatherResult` 天气详情文字兜底。
- 天气详情弹窗继续使用播报辅助层和聊天抑制生命周期，不改变实际 TTS 目标选择。

## 实际验证

- `node --test tests/weather-command.test.js tests/display-voice-tts-routing.test.js tests/display-chat-mmd.test.js tests/display-voice-ui.test.js tests/display-broadcast-text-rotation.test.js tests/display-voice-normal-chat.test.js`
- 结果：本轮相关测试 45 项全部通过；包含天气文本、显示端 TTS 弹窗、有/无语音能力兜底分流和聊天回传契约。

## 发布结果

- 初次修复版本已发布 code v20 到局域网和外网，随后因普通 response 弹窗遗漏补发 code v21 与 dependencies v5；未打包 APK。
- code v21 大小 `15,180,143` bytes，SHA-256 为 `098a0775c6a22977e3747512d3eaab48ce3bc8c0a32780dbc3995ee8c2ef49e8`。
- dependencies v5 大小 `117,378,766` bytes，SHA-256 为 `27d938041099dfae5819602870bed148202b86fd7414e952235962c2eeaa2547`；lockfile 指纹为 `d0d0d4af2176a1168411ceaca7d24f9e7dc52d8e5c20a1b3f0dd2b15e837626e`。
- 本次能力分流修复已发布 code v22：大小 `15,180,288` bytes，SHA-256 为 `fad58da51e3ef35dabda6edc4d63cbdee8422a9e8dc9e630c7cb2ece4a90921a`；沿用 dependencies v5，未打包 APK 或重新发布依赖包。
- 天气播报分流已发布 code v23：大小 `15,180,480` bytes，SHA-256 为 `98c5484c9ac234e8d84378fd730a06dea8ffe34fd44177d6bab510fcee9615dc`；沿用 dependencies v5，未打包 APK 或重新发布依赖包。
- 依赖包包含 `openai/_vendor/partial-json-parser/parser.mjs`，代码包和依赖包均通过 `unzip -tq`。
- LAN/WAN `manifest.json` 与本地签名清单一致并通过公钥验签；code、dependencies、apkMin 的 HTTP `HEAD` 状态和 `Content-Length` 均匹配，远端 code v23 SHA-256 与本地一致。
- LAN/WAN 旧 code v22 已按版本规则精确清理；dependencies v5、min APK v32 和完整 APK v23 保留，未触碰日志、模型、配置、任务和 results。
