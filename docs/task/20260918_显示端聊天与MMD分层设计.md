# 显示端聊天与 MMD/VRM 同位分层设计

## 任务状态

- 日期：2026-09-18
- 状态：设计已保存，代码尚未实现，等待进入实现阶段
- 预计工时：5～8 个工作日（协议与聊天壳 2～3 天、VRM/MMD 运行时 2～3 天、动作协议和回归 1～2 天）

## 任务描述

为浏览器显示端设计一个与控制端共享数据的聊天入口，并在同一全屏舞台中增加 VRM/MMD 角色显示和动作控制。媒体继续位于最底层，聊天与 MMD 共享屏幕位置并支持上下视觉层级切换；角色聊天页面顶部提供会话切换。

本任务只保存完整设计、实现规格和执行计划，不修改业务代码、模型、APK 或发布资源。

## Design 需求

- 设计文档：[display-chat-mmd.md](../design/display-chat-mmd.md)
- 显示端运行在浏览器，聊天入口提供对象选择和角色聊天页面。
- 角色聊天页面顶部提供角色与会话选择器，角色 A/B 的历史和上下文隔离。
- 控制端与显示端使用现有 WebSocket 聊天链路同步 `chatMessage`、流式回复、历史和会话切换。
- 当前首期沿用全局聊天上下文；按 `displayId` 独立会话留在后续任务。
- 媒体层为底层，VRM/MMD 和聊天使用同一个全屏舞台；默认 MMD 位于聊天下方，交互控制层始终在最上方。
- 采用 three-vrm 直接加载 VRM；通过 mmd-parser 和适配器播放 VMD/VRMA，并参考 MMDAgent-EX 命令语义。
- 高级动作使用模型无关的 `mmd.action.plan`；低级接口提供受限的 `MOTION_ADD`、`MOTION_DELETE`、`MODEL_BINDFACE`、`MODEL_BINDBONE` 兼容语义。
- 复杂动作生成通过异步 `mmd.motion.generate` 扩展，不能阻塞聊天首包。
- 资源优先使用本地离线包或服务端缓存，外部 VRoid 地址只作为初始登记来源。

## Spec 设计

- 伪代码文档：[display-chat-mmd.md](../spec/display-chat-mmd.md)
- 设计覆盖舞台层级、移动端动态高度、软键盘、高 DPI Canvas、对象选择、会话切换、WebSocket 同步、模型缓存、动作计划、低级命令和错误回退。
- `ChatContext` 绑定角色、会话、模式、目标和显示端上下文；服务端快照是权威来源。
- `MmdActionPlan` 只引用白名单资源和语义动作，不包含脚本、任意 URL、文件系统路径或 JavaScript。

## 实现计划

- 详细计划：[display-chat-mmd.md](../superpowers/plans/2026-09-18-display-chat-mmd.md)
- 计划顺序：协议和契约 → 显示端聊天壳 → VRM/MMD 运行时 → 动作协议 → 异步复杂动作 → 浏览器与 Android WebView 回归。

## 受影响的功能模块和代码

以下是计划中的影响范围，本次保存设计时未修改这些代码：

### 显示端

- `src/apps/web-mediacenter/ui/public/display.html`：增加聊天、MMD 同位舞台和交互控制入口。
- `src/apps/web-mediacenter/ui/public/css/display.css`：增加同位分层、动态视口、安全区、软键盘和高 DPI 样式。
- `src/apps/web-mediacenter/ui/public/js/display-chat.js`：计划新增，负责对象选择、会话和消息同步。
- `src/apps/web-mediacenter/ui/public/js/display-vrm.js`：计划新增，负责 VRM 加载、缓存、释放、表情和口型。
- `src/apps/web-mediacenter/ui/public/js/display-mmd-command-adapter.js`：计划新增，负责高级计划和 MMDAgent-EX 兼容命令。

### 服务端与协议

- `src/apps/server/boot/server-app.js`：复用现有聊天广播和显示端路由，必要时补充显示端聊天入口语义。
- `src/apps/server/modules/chat/` 及现有聊天历史/会话模块：补充角色会话快照、动作计划和去重契约。
- `src/framework/transport/ws/`：仅在现有 WebSocket 消息流程确有缺口时扩展，不新增独立 HTTP 聊天接口。

### 测试与资源

- 显示端静态契约、聊天同步、WebSocket、资源校验和动作白名单测试。
- three-vrm、mmd-parser、动作资源包和模型清单的依赖/构建配置。
- 角色模型、动作和许可证元数据清单；不在首期提交外部模型二进制。

## 自测用例

1. 显示端打开聊天入口，能看到角色/助手/群聊对象；无模型对象仍可聊天。
2. 进入角色后，会话选择器固定在顶部；切换角色或 session 后历史不串线。
3. 显示端发送消息，控制端能看到同一条消息；流式 `chatChunk` 和最终 `chatResponse` 两端一致。
4. 重复 `requestId`、过期 session 和断线重连不会重复插入消息。
5. 聊天隐藏、MMD 隐藏、MMD 上下层切换不影响媒体播放和输入控件操作。
6. 本地 VRM 加载成功时能播放待机和预置 VMD/VRMA；模型加载失败时聊天和媒体仍可用。
7. 高级动作计划能执行预置动作；非法资源、脚本、URL、越界骨骼和未知低级命令被拒绝。
8. think 内容按照显示端/控制端既有规则展示，TTS 不读取 think 标签，动作计划不进入聊天气泡。
9. 竖屏、软键盘、高 DPI、页面隐藏/恢复场景下无明显 UI 放大、遮挡或渲染循环泄漏。
10. 无外网时，已缓存模型和动作能运行；无模型时显示占位状态且聊天正常。

## 兼容性测试

- Chrome Android、Android WebView 和桌面 Chrome 的 WebSocket、WebGL/Canvas、动态视口和软键盘。
- 现有 `display.html` 媒体类型、旋转裁剪、睡眠模式、语音输入和 TTS 播放。
- 现有控制端群聊、私聊、角色切换、清空、think 分流和离线 APK 全局聊天上下文。
- VRM 0.0/1.0 资源、VMD/VRMA 预置动作和无模型聊天对象。
- 低端设备降级：无 WebGL、内存不足、资源超时、旧缓存回退和动作缺失。

## 性能测试

- 记录显示端首屏、聊天壳、角色列表、VRM 首次加载和首次动作播放的耗时分段。
- 测量隐藏 MMD 后 CPU/GPU 使用和渲染循环是否停止。
- 测量高 DPI 下 Canvas 实际像素尺寸、内存和帧率，确认 CSS 视觉尺寸不放大。
- 测量 1、10、50 条流式消息和 1、10 个动作轨道的消息处理与渲染延迟。
- 测量模型切换、动作释放和页面恢复后的内存是否回收。
- 复杂动作生成只测异步任务提交与资源就绪通知，不把生成耗时计入聊天首包。

## 风险评估

- VRM/VMD 骨骼和表情命名不一致：通过角色资源映射表和动作白名单处理，缺失轨道按部分播放降级。
- 全屏 Canvas 可能遮挡聊天：交互层固定最高层级，MMD Canvas 默认不接收聊天区域点击。
- 手机软键盘和高 DPI 可能导致舞台放大：使用动态视口、CSS 尺寸与实际绘制尺寸分离，并做 Android WebView 真机回归。
- 模型资源体积大：按角色懒加载，版本化缓存，首期不把所有模型放进首包。
- AI 动作计划可能包含危险字段：服务端和客户端双重校验，只允许资源 ID、语义动作和有限参数。
- 复杂动作生成延迟或失败：异步执行，立即返回聊天正文，使用待机动作和缓存回退。
- 当前全局聊天上下文与未来按 displayId 隔离存在迁移成本：将 displayId 作为可选上下文字段保留，避免重新设计消息结构。

## 预计工时

- 协议、数据模型和契约测试：1～2 个工作日。
- 显示端聊天入口、对象选择和会话同步：1～2 个工作日。
- three-vrm、动作播放器、资源缓存和移动端适配：2～3 个工作日。
- 高级/低级动作协议、安全校验和回退：1 个工作日。
- 浏览器、Android WebView、性能和回归测试：1～2 个工作日。

## 未明确事项

- 角色对象清单最终复用哪些现有模板字段，需要实现阶段按当前聊天配置确认。
- VMD 到具体 VRM 角色的骨骼和 morph 映射需要选定测试模型后补充资源清单。
- 复杂动作生成模型、运行位置和模型热更格式暂不绑定，需在首期运行时稳定后单独评审。
