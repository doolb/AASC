# Web MediaCenter - 未完成任务列表

## 代码重构
 - 拆分代码，分为核心代码和业务代码
   - 核心代码负责处理显示端和控制端的通信
   - 业务代码负责处理业务逻辑，如裁剪、播放视频等

## 重构聊天系统

> 设计文档：[design/chat-system.md](design/chat-system.md)
> 任务文档：[task/2026-03-29_聊天系统重构.md](task/2026-03-29_聊天系统重构.md)
> 实现文档：[spec/chat-system.md](spec/chat-system.md)

### 已实现功能
- [x] 提醒功能：`提醒{时间} {内容}`
- [x] 重复提醒：`每天/每周/每月/每年提醒{时间} {内容}`
- [x] 报时功能：`报时` / `现在几点`
- [x] 开启/关闭报时：`开启报时` / `关闭报时`
- [x] 搜索功能：`搜索{关键词}`
- [x] 取消操作：`拒绝` / `取消`
- [x] 指定助手对话：`{助手名字}{消息}`
- [x] 实现新的聊天记录数据结构和存储
- [x] 实现群聊/私聊模式切换
  - [x] `私聊{助手名字}` 指令
  - [x] `退出私聊` 指令
- [x] 实现系统指令功能
  - [x] `系统帮助` 指令
  - [x] 自定义指令配置
  - [x] 指令组合执行
- [x] 实现控制端语音播放功能
- [x] 重要记录功能：`系统记录{内容}`
- [x] 移除搜索页面，整合到聊天系统
- [x] 语音播报在显示端播报时同时显示文本

### 功能需求
- 现在的聊天分为群聊和私聊
- 聊天角色有系统，各个ai聊天助手，各个显示端，和控制端
- 显示端只能语音输入
- 控制端可以语音输入也可以文本输入
- 把现在现在语音播报都统计在聊天记录里
- 聊天记录格式 `角色: 消息`
- 系统消息：`系统: 消息`
- ai聊天助手消息：`（助手名字）: 消息`
- 显示端消息：`（显示端名字，ip）: 消息`
- 控制端消息：`（控制端名字，ip）: 消息`
- 移除现在搜索页面
- 文字聊天遵循语音聊天的规则，在系统规则内通过系统处理，其他相应名字的ai助手处理
  - 可以通过指令：`系统帮助` 来查看系统的指令帮助
  - 可以在控制端配置系统的指令，组合多个指令，如 "关键字：指令1，指令2，指令3"
    - 比如我说"早上好"，系统会执行指令1，指令2，指令3，比如"今天天气，今日提醒，搜索1，搜索2"，
- 私聊模式，只能和指定的ai助手私聊
  - 发起私聊时，需要指定私聊对象, 指令：`私聊（助手名字）`
  - 退出私聊时，指令：`退出私聊`， 或者和新的私聊对象私聊
  - 私聊时，只能和指定的私聊对象聊天
  - 私聊时，不能和其他ai助手聊天
- 语音播报新增一个可以在控制端播放语音功能
  - 当没有显示端时，需要在当前控制端播放语音
  - 当勾选了"在控制端播放语音"时，需要在当前控制端播放语音
- 语音播报在显示端播报时，同时还需要显示文本，文本内容是语音播报的内容
- 所有聊天记录都要保存到文件
- 重要记录，用户以"系统记录"开头，系统会自动保存到一个重要记录文件

## 显示端切换功能
 - 新增显示端全选和自适应功能
   - 全选时，下发到所有显示端
   - 自适应时，根据显示端画面比例（包含旋转角度）和媒体比例来下发媒体
   - 横向的媒体，下发到横向显示端
   - 纵向的媒体，下发到纵向显示端

## 重构显示端控制页面

> 设计文档：[design/display-control-refactor.md](design/display-control-refactor.md)
> 任务文档：[task/2026-03-29_显示端控制页面重构.md](task/2026-03-29_显示端控制页面重构.md)

### 已实现功能
- [x] 浮动控制面板：固定在页面右下角，可在所有页面访问
- [x] 播放命令：`播放{文件名}` 搜索并播放媒体文件
- [x] 天气语音播报：天气查询结果自动生成 TTS 语音播报
- [x] 自定义指令持久化：保存到 config/chat-commands.json
- [x] 通用时间解析功能：支持相对日期、相对时间、绝对日期等
- [x] 自测功能：控制端新增测试按钮，执行自测流程
- [x] 自测文档：包含自测流程、自测结果、自测注意事项等

### Bug 修复
- [x] 控制端，显示页面和快捷显示页面的播放按钮以及其他信息都未同步当前显示端的状态
  - 修复：floating-control.js 中调用 DisplayList.select 方法而非 selectDisplay
- [x] 修复90度和270度时，上下拖动裁剪框，显示端是左右方向的问题
  - 修复：在显示端 applyCrop 中根据旋转角度调整裁剪坐标
  - 改动文件：public/display.html
- [x] floating-control.js:66 Uncaught TypeError: window.DisplayList.selectDisplay is not a function
  - 修复：调用 DisplayList.select 方法而非 selectDisplay
- [x] 添加自定义指令配置文件，还是没有保存到 config/chat-commands.json
  - 修复：添加 WebSocketManager.send 通用方法，创建 chat-commands.json 初始文件
